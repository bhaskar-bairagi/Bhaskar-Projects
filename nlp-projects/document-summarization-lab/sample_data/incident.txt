The nightly payment settlement pipeline received 120 synthetic transactions. The validation step rejected five rows because transaction identifiers were duplicated. The remaining 115 rows were loaded into the local reporting table. A count check failed because the source count and loaded count differed. The operations team paused publication of the daily report. The team will correct the duplicate source records and rerun validation before publishing.
